/**
 * Topological ordering of stages from `depends_on`, with cycle detection.
 */
export function resolveStageOrder(stages: unknown[]): string[] {
  void stages;
  throw new Error("resolveStageOrder not implemented");
}
