/**
 * Parses and validates `.pipelineos.yml` contents against the Phase 1 schema.
 */
export function parsePipelineYaml(raw: string): unknown {
  void raw;
  throw new Error("parsePipelineYaml not implemented");
}
